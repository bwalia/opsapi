app:
  env: test
  name: opsapi-node
  image:
    repository: bwalia/opsapi-node
    tag: latest
    pullPolicy: Always
# imagePullSecrets: []
# nameOverride: ""
# fullnameOverride: ""

replicaCount: 1

secrets:
  env_file: null

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: test-opsapi-node.workstation.co.uk
      paths:
        - path: /
          pathType: Prefix
  tls: []

env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: 3000

resources: {}
